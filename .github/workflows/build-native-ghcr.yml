# build-native-ghcr.yml
name: Build and Push Native Image to GHCR
on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # 允许手动触发

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }} # 使用 GitHub 仓库名称作为镜像名
  JAVA_VERSION: '25' # GraalVM JDK 版本

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository # 拉取代码
        uses: actions/checkout@v4

      - name: Set up GraalVM # 安装 GraalVM，提供 native-image
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Maven repository # 加速依赖下载
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Extract metadata (tags, labels) for Docker # 提取 Docker 镜像标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Build native image with Spring Boot # 构建 OCI 镜像 (GraalVM native)
        env:
          BP_JVM_VERSION: 25 # Spring Boot 4 需要 NIK/GraalVM 25+，避免 buildpack 报版本过低
        run: ./mvnw -Pnative spring-boot:build-image -DskipTests -Dspring-boot.build-image.imageName=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tags }}

      - name: Log in to GitHub Container Registry # 登录到 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to GitHub Container Registry # 推送到 GHCR
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tags }}

      - name: Print deployed container information # 打印部署的容器信息
        run: |
          echo "✅ Docker 镜像构建并推送成功！"
          echo "📦 镜像名称: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "🏷️  镜像标签: ${{ steps.meta.outputs.tags }}"
          echo ""
          echo "🚀 拉取镜像: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tags }}"
          echo "🚀 运行镜像: docker run -p 4567:4567 ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tags }}"
          echo ""
          echo "🌐 部署完成，镜像已推送到 GitHub Container Registry (GHCR)"