server:
  port: 4567

spring:
  application:
    name: ServiceLinkLite
  threads:
    virtual:
      enabled: true

feedback:
  enabled: true
  dingTalk:
    webhook: ""
    secret: ""
  rate-limit:
    enabled: true
    max-requests: 5
    window-minutes: 60
