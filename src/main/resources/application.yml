server:
  port: 4567

spring:
  application:
    name: ServiceLinkLite
  threads:
    virtual:
      enabled: true

feedback:
  enabled: true
  dingTalk:
    webhook: ""
    secret: ""
  rate-limit:
    enabled: true
    limit-per-minute: 10
